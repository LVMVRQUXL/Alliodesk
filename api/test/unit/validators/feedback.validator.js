const {describe, it} = require('mocha');
const assert = require('assert');

module.exports = () => describe('FeedbackValidator', () => {
    describe('test', () => { // TODO: delete this example
        it('should pass', () => {
            assert.equal(1, 1);
        });
    });
});
